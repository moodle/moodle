issueNumber: MDL-82494
notes:
  core_user:
    - message: >
        The user profile field `display_name()` method now accepts an optional
        `$escape` parameter to define whether to escape the returned name
      type: changed

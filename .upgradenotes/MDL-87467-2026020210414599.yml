issueNumber: MDL-87467
notes:
  core_course:
    - message: >-
        Add new routes for course module, so we can predict what would be the
        next page to show when using course linear navigation. The route url
        will be like course/cms/<cmid>/next. If there is not next page, this
        will return a 404 error.
      type: improved

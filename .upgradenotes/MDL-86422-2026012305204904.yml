issueNumber: MDL-86422
notes:
  tool_task:
    - message: Change semantic of queue_adhoc_task so now it always returns the task id of newly inserted task or existing task (depending on the $checkforexisting) or false if the task component is deprecated or the task could not be queued due to DML error.
      type: fixed

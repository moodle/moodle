define("aiprovider_anthropic/modelchooser",["exports"],(function(_exports){Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0;
/**
   * AI provider model selection handler.
   *
   * Unlike aiprovider_gemini, this provider has a single shared API endpoint, so this module
   * only needs to keep the hidden model field in step with the selection and resubmit the form
   * so the server-side per-model settings (e.g. temperature) can be re-evaluated.
   *
   * @module     aiprovider_anthropic/modelchooser
   * @copyright  2026 Matt Porritt <matt.porritt@moodle.com>
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */
const Selectors_fields={selector:'[data-modelchooser-field="selector"]',updateButton:'[data-modelchooser-field="updateButton"]',model:'input[name="model"]',custommodel:'input[name="custommodel"]'};_exports.init=()=>{const modelSelector=document.querySelector(Selectors_fields.selector);modelSelector&&modelSelector.addEventListener("change",(e=>{const form=e.target.closest("form"),selectedModel=e.target.value,modelField=form.querySelector(Selectors_fields.model);if(modelField)if("custom"===selectedModel){const customModelField=form.querySelector(Selectors_fields.custommodel);modelField.value=customModelField?customModelField.value:""}else modelField.value=selectedModel;form.querySelector(Selectors_fields.updateButton).click()}))}}));

//# sourceMappingURL=modelchooser.min.js.map
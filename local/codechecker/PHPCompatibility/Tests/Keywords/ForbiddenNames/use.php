<?php
use Baz;
use abstract;
use and;
use array;
use as;
use break;
use callable;
use case;
use catch;
use class;
use clone;
use const;
use continue;
use declare;
use default;
use die;
use do;
use echo;
use else;
use elseif;
use empty;
use enddeclare;
use endfor;
use endforeach;
use endif;
use endswitch;
use endwhile;
use eval;
use exit;
use extends;
use final;
use finally;
use for;
use foreach;
use function;
use global;
use goto;
use if;
use implements;
use include;
use include_once;
use instanceof;
use insteadof;
use interface;
use isset;
use list;
use namespace;
use new;
use or;
use print;
use private;
use protected;
use public;
use require;
use require_once;
use return;
use static;
use switch;
use throw;
use trait;
use try;
use unset;
use use;
use var;
use while;
use xor;
use yield;
use __CLASS__;
use __DIR__;
use __FILE__;
use __FUNCTION__;
use __METHOD__;
use __NAMESPACE__;
